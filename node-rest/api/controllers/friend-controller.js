const mongoose = require('mongoose');

const User = require('../models/User');
const Friend = require('../models/Friend');
const config = require('../../config/config');


//TODO: Friends services
exports.friend_get_friends = {
}
